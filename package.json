{
  "name": "seatflow",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@11.17.0",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "node --test tests/repository.test.mjs",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "9.1.7",
    "lint-staged": "17.2.0",
    "prettier": "3.9.6"
  },
  "lint-staged": {
    "*": "prettier --ignore-unknown --write"
  }
}
