global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  # local api is on the host gateway
  - job_name: event-ticketing-api
    metrics_path: /metrics
    static_configs:
      - targets: ["host.docker.internal:4000"]
