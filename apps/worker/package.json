{
  "name": "@event-ticketing/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsx watch src/main.ts",
    "lint": "eslint src",
    "start": "node dist/main.js",
    "test": "vitest run src",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@event-ticketing/config": "workspace:*",
    "@event-ticketing/database": "workspace:*",
    "@event-ticketing/payments": "workspace:*",
    "nodemailer": "9.0.3"
  },
  "devDependencies": {
    "@types/nodemailer": "8.0.1",
    "@types/pg": "8.20.0",
    "tsx": "4.23.1"
  }
}
